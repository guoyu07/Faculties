import {UserScore} from './UserScore';

export class Group {
  id: number;
  information: string;
  limit: number;
  users: number[];
  enrollMark: number;
  facultyId: number;
  issueDate: Date;
  qualify: string;
  subjectNames: String[];
}
